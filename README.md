# EPOCH-FRAMEWORK
A lightweight, hybrid (local + LLM), external safety guard that operates exclusively through public APIs.   The framework enforces structured invariants on model outputs without modifying model weights, while preserving human agency, auditability, and regulatory compliance.
